experiment:
  name: unet_v4_1m_mae

data:
  root: "E:/PycharmProjects/SnowPrediction/dataset_v4_fisico"
  csv_file: "dataset_v4_fisico.csv"
  images_dir: "images"
  masks_dir: "masks"
  source: "lidar"
  split_type: "temporal"
  use_sce: false

model:
  architecture: "unet"
  in_channels: 5
  out_channels: 1

training:
  batch_size: 16
  learning_rate: 0.0001
  epochs: 50
  loss: "mae"
  num_workers: 0
  device: "auto"

output:
  models_dir: "E:/PycharmProjects/SnowPrediction/Articulo 1/Models"
  results_dir: "E:/PycharmProjects/SnowPrediction/results/unet_v4_1m_mae"
  model_name: "e2_b02_1_best_unet_lidar"
